{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only the bundled NetWatch dashboard may use Tauri core APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
